import messageQueue from "../src/components/classes/systems/messageQueue";

describe('should manage and send all message recieved to the proper recipients',()=>{
       test('placeHolder',()=>expect(1).toBe(1))
})