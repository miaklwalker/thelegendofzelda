import SpriteSheet from "../src/components/classes/systems/SpriteSheet";

describe('Should return a class with methods and properties for animating sprites',()=>{
        test('placeHolder',()=>expect(1).toBe(1))
})