import message from "../src/components/classes/systems/message";

describe('Should return a class containing the properties: to , from , data , type',()=>{
       test('placeHolder',()=>expect(1).toBe(1))
})