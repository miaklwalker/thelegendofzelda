import pauseScreen from "../src/components/classes/systems/pauseScreen"

describe('Should bring up a pause menu when enter is pressed that has inventory and hud',()=>{
       test('placeHolder',()=>expect(1).toBe(1))
})